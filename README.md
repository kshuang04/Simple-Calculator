## Calculators

To run the calculator that uses the Python tkinter library, run `$ python tkinter/calculator.py`

To run the calclator that uses javascipt, run `$ npx serve` in the `javascript` directory

The JS calculator follows this tutorial: https://www.youtube.com/watch?v=I5kj-YsmWjM&t=1s